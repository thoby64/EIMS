@extends('layouts.app')
@section('title','Asset Requests')
@section('heading','Asset requests')
@section('content')<div class="pt-4"><section class="eims-card overflow-hidden"><div class="flex flex-col gap-4 border-b p-5 lg:flex-row lg:items-center"><div><h2 class="font-extrabold text-eims-ink">{{ auth()->user()->hasPermission('requests.approve')?'Procurement request queue':'My property requests' }}</h2><p class="text-xs text-slate-400">Asset requests are managed independently from maintenance spare requisitions.</p></div><form method="GET" class="eims-filter-panel ml-auto grid gap-3 sm:grid-cols-3 sm:items-end"><label class="eims-filter-label">Search requests<input name="search" value="{{ request('search') }}" placeholder="Reference, category, purpose or officer" class="eims-filter-field min-w-56"></label><label class="eims-filter-label">Category<select name="category" class="eims-filter-field"><option value="">All categories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('category')==$category->id)>{{ $category->name }}</option>@endforeach</select></label><label class="eims-filter-label">Status<select name="status" class="eims-filter-field"><option value="">All statuses</option>@foreach(['submitted','approved','rejected','allocated'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select></label><div class="flex gap-2 sm:col-span-3 sm:justify-end"><button class="eims-filter-button">Apply filters</button>@if(request()->hasAny(['search','category','status']))<a href="{{ route('requests.index') }}" class="eims-filter-reset">Clear</a>@endif</div></form>@if(auth()->user()->hasPermission('requests.create'))<a href="{{ route('requests.create') }}" class="rounded-xl bg-gradient-to-r from-orange-500 to-red-500 px-5 py-3 text-center text-sm font-bold text-white shadow-lg">New request</a>@endif</div><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead><tr><th class="px-6">Request</th><th class="px-6">Category</th><th class="px-6">Requester</th><th class="px-6">Purpose</th><th class="px-6">Status</th><th></th></tr></thead><tbody>@forelse($requests as $request)<tr><td class="px-6 py-4 font-mono text-xs font-bold text-indigo-500">{{ $request->request_number }}</td><td class="px-6 py-4 font-bold text-eims-ink">{{ $request->category->name }}</td><td class="px-6 py-4">{{ $request->requester->name }}</td><td class="max-w-md px-6 py-4 text-slate-500">{{ $request->purpose }}</td><td class="px-6 py-4"><span class="rounded-full bg-indigo-50 px-3 py-1 text-xs font-bold capitalize text-indigo-600">{{ $request->status }}</span></td><td class="px-6 py-4 text-right"><a href="{{ route('requests.show',$request) }}" class="font-bold text-indigo-600">View →</a></td></tr>@empty<tr><td colspan="6" class="p-16 text-center text-slate-400">No asset requests match these filters.</td></tr>@endforelse</tbody></table></div>@if($requests->hasPages())<div class="border-t p-5">{{ $requests->links() }}</div>@endif</section></div>@endsection
