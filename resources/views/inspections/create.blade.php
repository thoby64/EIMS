@extends('layouts.app') @section('title','Schedule inspection · EIMS') @section('heading','Schedule inspection')
@section('content')<form method="POST" action="{{ route('inspections.store') }}" class="eims-card mx-auto max-w-3xl space-y-5 p-6">@csrf<h2 class="font-extrabold">Inspection assignment</h2><label class="block text-sm font-bold">Asset<select name="asset_id" required class="mt-2 w-full rounded-xl border-slate-200"><option value="">Select asset</option>@foreach($assets as $asset)<option value="{{ $asset->id }}" @selected($selectedAsset?->id===$asset->id)>{{ $asset->asset_tag }} — {{ $asset->name }}</option>@endforeach</select></label><label class="block text-sm font-bold">Inspection officer<select name="inspector_user_id" required class="mt-2 w-full rounded-xl border-slate-200"><option value="">Select officer</option>@foreach($inspectors as $inspector)<option value="{{ $inspector->id }}">{{ $inspector->name }} — {{ $inspector->staff_number }}</option>@endforeach</select></label><label class="block text-sm font-bold">Scheduled date and time<input type="datetime-local" name="scheduled_at" required class="mt-2 w-full rounded-xl border-slate-200"></label>@if($errors->any())<div class="rounded-xl bg-red-50 p-4 text-sm text-red-700">{{ $errors->first() }}</div>@endif<div class="flex justify-end"><button class="rounded-xl bg-indigo-600 px-6 py-3 text-sm font-bold text-white">Schedule and notify officer</button></div></form>@endsection
