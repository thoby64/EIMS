@extends('layouts.app')
@section('title','Inspections · EIMS')
@section('heading','Asset inspections')
@section('content')<div class="eims-card overflow-hidden"><div class="flex flex-col gap-4 border-b p-6 lg:flex-row lg:items-center"><div><h2 class="font-extrabold">Inspection register</h2><p class="text-sm text-slate-400">Physical presence, location, custody and condition verification.</p></div><form method="GET" class="eims-filter-panel ml-auto flex flex-col gap-3 sm:flex-row sm:items-end"><label class="eims-filter-label">Search inspections<input name="search" value="{{ request('search') }}" placeholder="Reference, asset, tag or inspector" class="eims-filter-field min-w-64"></label><label class="eims-filter-label">Status<select name="status" class="eims-filter-field min-w-44"><option value="">All statuses</option>@foreach(['scheduled','completed','cancelled'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select></label><button class="eims-filter-button">Apply filters</button>@if(request()->hasAny(['search','status']))<a href="{{ route('inspections.index') }}" class="eims-filter-reset">Clear</a>@endif</form>@if(auth()->user()->hasPermission('inspections.manage'))<a href="{{ route('inspections.create') }}" class="rounded-xl bg-indigo-600 px-5 py-3 text-center text-sm font-bold text-white">Schedule inspection</a>@endif</div><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-400"><tr><th class="px-6 py-3">Inspection</th><th class="px-6 py-3">Asset</th><th class="px-6 py-3">Inspector</th><th class="px-6 py-3">Scheduled</th><th class="px-6 py-3">Status</th><th></th></tr></thead><tbody class="divide-y">@forelse($inspections as $inspection)<tr><td class="px-6 py-4 font-mono text-xs text-indigo-500">{{ $inspection->inspection_number }}</td><td class="px-6 py-4"><strong>{{ $inspection->asset->name }}</strong><p class="text-xs text-slate-400">{{ $inspection->asset->asset_tag }}</p></td><td class="px-6 py-4">{{ $inspection->inspector->name }}</td><td class="px-6 py-4">{{ $inspection->scheduled_at->format('d M Y, H:i') }}</td><td class="px-6 py-4 capitalize">{{ $inspection->status }}</td><td class="px-6 py-4 text-right"><a href="{{ route('inspections.show',$inspection) }}" class="font-bold text-indigo-600">View →</a></td></tr>@empty<tr><td colspan="6" class="p-16 text-center text-slate-400">No inspections match these filters.</td></tr>@endforelse</tbody></table></div>@if($inspections->hasPages())<div class="border-t p-5">{{ $inspections->links() }}</div>@endif</div>@endsection
