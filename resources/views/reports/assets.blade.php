@extends('layouts.app') @section('title','Asset register report · EIMS') @section('heading','Asset register report')
@section('content')<div class="eims-card overflow-hidden"><form method="GET" class="grid gap-3 border-b p-5 md:grid-cols-4"><input name="q" value="{{ request('q') }}" placeholder="Tag, name or serial" class="rounded-xl border-slate-200"><select name="category" class="rounded-xl border-slate-200"><option value="">All categories</option>@foreach($categories as $x)<option value="{{ $x->id }}" @selected(request('category')==$x->id)>{{ $x->name }}</option>@endforeach</select><select name="department" class="rounded-xl border-slate-200"><option value="">All departments</option>@foreach($departments as $x)<option value="{{ $x->id }}" @selected(request('department')==$x->id)>{{ $x->name }}</option>@endforeach</select><select name="location" class="rounded-xl border-slate-200"><option value="">All locations</option>@foreach($locations as $x)<option value="{{ $x->id }}" @selected(request('location')==$x->id)>{{ $x->name }}</option>@endforeach</select><select name="status" class="rounded-xl border-slate-200"><option value="">All statuses</option>@foreach(['in_stock','reserved','assigned','under_maintenance','awaiting_disposal','retired','disposed'] as $x)<option value="{{ $x }}" @selected(request('status')===$x)>{{ ucfirst(str_replace('_',' ',$x)) }}</option>@endforeach</select><select name="condition" class="rounded-xl border-slate-200"><option value="">All conditions</option>@foreach(['excellent','good','fair','damaged'] as $x)<option value="{{ $x }}" @selected(request('condition')===$x)>{{ ucfirst($x) }}</option>@endforeach</select><button class="rounded-xl bg-indigo-600 px-5 py-2 text-sm font-bold text-white">Apply filters</button><a href="{{ route('reports.assets.export',request()->query()) }}" class="rounded-xl bg-emerald-600 px-5 py-2 text-center text-sm font-bold text-white">Export filtered CSV</a></form><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-400"><tr><th class="px-5 py-3">Asset</th><th class="px-5 py-3">Category</th><th class="px-5 py-3">Custody</th><th class="px-5 py-3">Location</th><th class="px-5 py-3">Condition</th><th class="px-5 py-3">Status</th></tr></thead><tbody class="divide-y">@forelse($assets as $asset)<tr><td class="px-5 py-4"><strong>{{ $asset->name }}</strong><p class="font-mono text-xs text-indigo-500">{{ $asset->asset_tag }}</p></td><td class="px-5 py-4">{{ $asset->category->name }}</td><td class="px-5 py-4">{{ $asset->custodian?->name ?? $asset->custodianDepartment?->name ?? 'University stock' }}</td><td class="px-5 py-4">{{ $asset->location?->name ?? '—' }}</td><td class="px-5 py-4 capitalize">{{ $asset->condition }}</td><td class="px-5 py-4 capitalize">{{ str_replace('_',' ',$asset->lifecycle_status) }}</td></tr>@empty<tr><td colspan="6" class="p-16 text-center text-slate-400">No assets match these filters.</td></tr>@endforelse</tbody></table></div>@if($assets->hasPages())<div class="border-t p-5">{{ $assets->links() }}</div>@endif</div>@endsection
