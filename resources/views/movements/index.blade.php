@extends('layouts.app')
@section('title','Returns and transfers · EIMS')
@section('heading','Returns and transfers')
@section('content')
<div class="eims-card overflow-hidden"><div class="flex flex-col gap-4 border-b p-6 lg:flex-row lg:items-center"><div><h2 class="font-extrabold text-eims-ink">Custody movement register</h2><p class="mt-1 text-sm text-slate-400">Approval, physical handover, receipt confirmation and final custody.</p></div><form method="GET" class="eims-filter-panel ml-auto grid gap-3 sm:grid-cols-3 sm:items-end"><label class="eims-filter-label">Search<input name="search" value="{{ request('search') }}" placeholder="Reference, asset or tag" class="eims-filter-field min-w-52"></label><label class="eims-filter-label">Movement type<select name="type" class="eims-filter-field"><option value="">All types</option><option value="transfer" @selected(request('type')==='transfer')>Transfer</option><option value="return" @selected(request('type')==='return')>Return</option></select></label><label class="eims-filter-label">Status<select name="status" class="eims-filter-field"><option value="">All statuses</option>@foreach(['pending_procurement','awaiting_receipt','completed','rejected'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst(str_replace('_',' ',$status)) }}</option>@endforeach</select></label><div class="flex gap-2 sm:col-span-3 sm:justify-end"><button class="eims-filter-button">Apply filters</button>@if(request()->hasAny(['search','type','status']))<a href="{{ route('movements.index') }}" class="eims-filter-reset">Clear</a>@endif</div></form></div><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-400"><tr><th class="px-6 py-3">Movement</th><th class="px-6 py-3">Asset</th><th class="px-6 py-3">Type</th><th class="px-6 py-3">Initiated by</th><th class="px-6 py-3">Status</th><th></th></tr></thead><tbody class="divide-y">@forelse($movements as $movement)<tr><td class="px-6 py-4 font-mono text-xs font-bold text-indigo-500">{{ $movement->movement_number }}</td><td class="px-6 py-4"><p class="font-bold text-eims-ink">{{ $movement->asset->name }}</p><p class="text-xs text-slate-400">{{ $movement->asset->asset_tag }}</p></td><td class="px-6 py-4 capitalize">{{ $movement->type }}</td><td class="px-6 py-4">{{ $movement->initiator->name }}</td><td class="px-6 py-4"><span class="rounded-full bg-indigo-50 px-3 py-1 text-xs font-bold capitalize text-indigo-600">{{ str_replace('_',' ',$movement->status) }}</span></td><td class="px-6 py-4 text-right"><a class="font-bold text-indigo-600" href="{{ route('movements.show',$movement) }}">View →</a></td></tr>@empty<tr><td colspan="6" class="p-16 text-center text-slate-400">No custody movements match these filters.</td></tr>@endforelse</tbody></table></div>@if($movements->hasPages())<div class="border-t p-5">{{ $movements->links() }}</div>@endif</div>
@endsection
