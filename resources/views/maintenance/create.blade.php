@extends('layouts.app')
@section('title','Report Asset Problem') @section('heading','Report asset problem')
@section('content')<div class="mx-auto max-w-3xl pt-4"><form method="POST" action="{{ route('maintenance.store') }}" class="eims-card space-y-5 p-6">@csrf<h2 class="text-xl font-black text-eims-ink">What happened to your assigned property?</h2>@if($errors->any())<div class="rounded-xl bg-red-50 p-4 text-sm text-red-600">{{ $errors->first() }}</div>@endif<div><label class="text-sm font-bold">Your assigned asset *</label><select name="asset_id" required class="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3"><option value="">Select asset</option>@foreach($assets as $asset)<option value="{{ $asset->id }}">{{ $asset->asset_tag }} · {{ $asset->name }}</option>@endforeach</select></div><div><label class="text-sm font-bold">Problem summary *</label><input name="problem_summary" required maxlength="500" class="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3"></div><div><label class="text-sm font-bold">Detailed description *</label><textarea name="problem_details" required rows="5" class="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3"></textarea></div><div><label class="text-sm font-bold">Severity *</label><select name="severity" class="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3">@foreach(['low','normal','high','critical'] as $s)<option value="{{ $s }}">{{ ucfirst($s) }}</option>@endforeach</select></div><div class="flex justify-end"><button class="rounded-xl bg-gradient-to-r from-rose-500 to-pink-600 px-6 py-3 text-sm font-extrabold text-white">Submit maintenance case</button></div></form></div>@endsection
