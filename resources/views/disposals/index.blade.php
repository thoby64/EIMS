@extends('layouts.app')
@section('title','Retirement & disposal · EIMS')
@section('heading','Retirement and disposal')
@section('content')<div class="eims-card overflow-hidden"><div class="flex flex-col gap-4 border-b p-6 lg:flex-row lg:items-center"><div><h2 class="font-extrabold">Controlled disposal register</h2><p class="text-sm text-slate-400">Independent review, Procurement decision, surrender and permanent finalization.</p></div><form method="GET" class="eims-filter-panel ml-auto grid gap-3 sm:grid-cols-3 sm:items-end"><label class="eims-filter-label">Search<input name="search" value="{{ request('search') }}" placeholder="Reference, asset or tag" class="eims-filter-field min-w-52"></label><label class="eims-filter-label">Reason<select name="reason" class="eims-filter-field"><option value="">All reasons</option>@foreach(['beyond_repair','obsolete','unsafe','missing','end_of_service','uneconomical_to_repair'] as $reason)<option value="{{ $reason }}" @selected(request('reason')===$reason)>{{ ucfirst(str_replace('_',' ',$reason)) }}</option>@endforeach</select></label><label class="eims-filter-label">Status<select name="status" class="eims-filter-field"><option value="">All statuses</option>@foreach(['pending_review','pending_approval','awaiting_surrender','ready_for_finalization','completed','review_rejected','approval_rejected'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst(str_replace('_',' ',$status)) }}</option>@endforeach</select></label><div class="flex gap-2 sm:col-span-3 sm:justify-end"><button class="eims-filter-button">Apply filters</button>@if(request()->hasAny(['search','reason','status']))<a href="{{ route('disposals.index') }}" class="eims-filter-reset">Clear</a>@endif</div></form></div><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-400"><tr><th class="px-6 py-3">Reference</th><th class="px-6 py-3">Asset</th><th class="px-6 py-3">Reason</th><th class="px-6 py-3">Proposer</th><th class="px-6 py-3">Status</th><th></th></tr></thead><tbody class="divide-y">@forelse($disposals as $d)<tr><td class="px-6 py-4 font-mono text-xs text-indigo-500">{{ $d->disposal_number }}</td><td class="px-6 py-4"><strong>{{ $d->asset->name }}</strong><p class="text-xs text-slate-400">{{ $d->asset->asset_tag }}</p></td><td class="px-6 py-4 capitalize">{{ str_replace('_',' ',$d->reason) }}</td><td class="px-6 py-4">{{ $d->proposer->name }}</td><td class="px-6 py-4 capitalize">{{ str_replace('_',' ',$d->status) }}</td><td class="px-6 py-4 text-right"><a href="{{ route('disposals.show',$d) }}" class="font-bold text-indigo-600">View →</a></td></tr>@empty<tr><td colspan="6" class="p-16 text-center text-slate-400">No retirement proposals match these filters.</td></tr>@endforelse</tbody></table></div>@if($disposals->hasPages())<div class="border-t p-5">{{ $disposals->links() }}</div>@endif</div>@endsection
