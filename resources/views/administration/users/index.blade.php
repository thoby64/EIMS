@extends('layouts.app')
@section('heading','Administration')
@section('content')<div class="space-y-5 pt-4"><nav class="flex gap-2"><a href="{{ route('administration.users.index') }}" class="rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-bold text-white">Staff accounts</a><a href="{{ route('administration.departments.index') }}" class="rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-bold text-slate-600">Departments</a></nav>@if(session('success'))<div class="rounded-xl bg-emerald-50 px-4 py-3 text-sm font-bold text-emerald-700">{{ session('success') }}</div>@endif<section class="eims-card overflow-hidden"><div class="flex flex-col gap-4 border-b p-5 lg:flex-row lg:items-center"><div><h2 class="font-extrabold">Officer accounts</h2><p class="text-xs text-slate-400">Manage identity, access, responsibilities and password resets.</p></div><form method="GET" class="eims-filter-panel ml-auto grid gap-3 sm:grid-cols-3 sm:items-end"><label class="eims-filter-label">Search staff<input name="search" value="{{ request('search') }}" placeholder="Name, staff number or email" class="eims-filter-field min-w-56"></label><label class="eims-filter-label">Department<select name="department" class="eims-filter-field"><option value="">All departments</option>@foreach($departments as $department)<option value="{{ $department->id }}" @selected(request('department')==$department->id)>{{ $department->name }}</option>@endforeach</select></label><label class="eims-filter-label">Account status<select name="status" class="eims-filter-field"><option value="">All statuses</option>@foreach(['active','inactive','suspended'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select></label><div class="flex gap-2 sm:col-span-3 sm:justify-end"><button class="eims-filter-button">Apply filters</button>@if(request()->hasAny(['search','department','status']))<a href="{{ route('administration.users.index') }}" class="eims-filter-reset">Clear</a>@endif</div></form><a href="{{ route('administration.users.create') }}" class="rounded-xl bg-indigo-600 px-5 py-2.5 text-center text-sm font-extrabold text-white">Register officer</a></div><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-400"><tr><th class="px-6 py-4">Officer</th><th class="px-6 py-4">Department</th><th class="px-6 py-4">Role</th><th class="px-6 py-4">Status</th><th></th></tr></thead><tbody class="divide-y">@forelse($users as $user)<tr><td class="px-6 py-4"><p class="font-bold">{{ $user->name }}</p><p class="text-xs text-slate-400">{{ $user->staff_number }} · {{ $user->email }}</p></td><td class="px-6 py-4">{{ $user->department?->name ?? 'Not assigned' }}</td><td class="px-6 py-4">@foreach($user->roles as $role)<span class="rounded-full bg-indigo-50 px-3 py-1 text-xs font-bold text-indigo-600">{{ $role->name }}</span>@endforeach</td><td class="px-6 py-4 capitalize">{{ $user->status }}</td><td class="px-6 py-4 text-right"><a href="{{ route('administration.users.edit',$user) }}" class="font-bold text-indigo-600">Manage →</a></td></tr>@empty<tr><td colspan="5" class="p-16 text-center text-slate-400">No staff accounts match these filters.</td></tr>@endforelse</tbody></table></div>@if($users->hasPages())<div class="border-t p-5">{{ $users->links() }}</div>@endif</section></div>@endsection
