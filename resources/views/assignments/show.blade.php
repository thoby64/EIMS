@extends('layouts.app')
@section('title','Assignment')
@section('heading','Assignment details')
@section('content')
<div class="mx-auto max-w-5xl pt-4">@if(session('success'))<div class="mb-5 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-bold text-emerald-700">{{ session('success') }}</div>@endif
<div class="grid gap-6 lg:grid-cols-[1.4fr_1fr]"><section class="eims-card p-6"><p class="font-mono text-xs font-bold text-indigo-500">{{ $assignment->asset->asset_tag }}</p><h2 class="mt-1 text-2xl font-black text-eims-ink">{{ $assignment->asset->name }}</h2><p class="mt-2 text-sm text-slate-400">{{ $assignment->asset->category->group->name }} · {{ $assignment->asset->category->name }}</p><dl class="mt-6 grid gap-5 border-t border-slate-100 pt-5 sm:grid-cols-2">@foreach([['Recipient',$assignment->assignee?->name],['Receiving location',$assignment->location?->name],['Prepared by',$assignment->assigner->name],['Condition at issue',ucfirst($assignment->condition_at_issue)],['Prepared on',$assignment->assigned_at->format('d M Y, H:i')],['Expected return',optional($assignment->expected_return_at)->format('d M Y')]] as [$label,$value])<div><dt class="text-[10px] font-bold uppercase tracking-wider text-slate-400">{{ $label }}</dt><dd class="mt-1 text-sm font-bold text-eims-ink">{{ $value ?: 'Not specified' }}</dd></div>@endforeach</dl><div class="mt-6 rounded-xl bg-slate-50 p-4"><p class="text-xs font-bold text-slate-400">Purpose</p><p class="mt-2 text-sm text-eims-ink">{{ $assignment->purpose }}</p></div>@if($assignment->accessories)<div class="mt-5"><p class="text-xs font-bold text-slate-400">Included accessories</p><div class="mt-2 flex flex-wrap gap-2">@foreach($assignment->accessories as $accessory)<span class="rounded-full bg-indigo-50 px-3 py-1 text-xs font-bold text-indigo-600">{{ $accessory }}</span>@endforeach</div></div>@endif</section>
<aside class="eims-card p-6"><p class="text-xs font-bold uppercase tracking-wider text-slate-400">Current status</p><p class="mt-2 inline-block rounded-full bg-indigo-50 px-4 py-2 text-sm font-extrabold capitalize text-indigo-600">{{ str_replace('_',' ',$assignment->status) }}</p>@if($assignment->receipt)<div class="mt-6 border-t border-slate-100 pt-5"><h3 class="font-extrabold text-eims-ink">Recipient confirmation</h3><dl class="mt-4 space-y-4"><div><dt class="text-xs text-slate-400">Decision</dt><dd class="font-bold capitalize text-eims-ink">{{ str_replace('_',' ',$assignment->receipt->decision) }}</dd></div><div><dt class="text-xs text-slate-400">Handed over by</dt><dd class="font-bold text-eims-ink">{{ $assignment->receipt->handedOverBy->name }}</dd></div><div><dt class="text-xs text-slate-400">Matches expected property</dt><dd class="font-bold text-eims-ink">{{ $assignment->receipt->matches_expected_asset ? 'Yes' : 'No' }}</dd></div><div><dt class="text-xs text-slate-400">Remarks</dt><dd class="text-sm text-eims-ink">{{ $assignment->receipt->remarks ?: 'None' }}</dd></div></dl></div>@else<p class="mt-5 text-sm leading-6 text-slate-400">Waiting for {{ $assignment->assignee?->name }} to inspect and confirm receipt.</p>@endif</aside></div></div>
@endsection
