@props(['data'])
@if(is_array($data) && count($data))
<dl class="grid gap-3 sm:grid-cols-2">@foreach($data as $key => $value)<div class="rounded-xl border border-slate-100 bg-slate-50/80 p-4 {{ is_array($value) ? 'sm:col-span-2' : '' }}"><dt class="text-[10px] font-black uppercase tracking-wider text-slate-400">{{ is_string($key) ? \Illuminate\Support\Str::headline($key) : 'Item '.($key + 1) }}</dt><dd class="mt-2 text-sm font-semibold text-eims-ink">@if(is_array($value))<x-audit-data :data="$value" />@elseif(is_bool($value))<span class="rounded-full px-3 py-1 text-xs {{ $value ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-200 text-slate-600' }}">{{ $value ? 'Yes' : 'No' }}</span>@elseif(is_null($value) || $value==='')<span class="font-normal text-slate-400">Not recorded</span>@else<span class="break-words">{{ $value }}</span>@endif</dd></div>@endforeach</dl>
@else<p class="rounded-xl bg-slate-50 p-5 text-sm text-slate-400">No additional information was recorded.</p>@endif
