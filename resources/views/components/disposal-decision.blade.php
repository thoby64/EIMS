<form method="POST" action="{{ $action }}" class="eims-card space-y-4 p-6">@csrf<h3 class="font-extrabold">{{ $title }}</h3><textarea name="comments" required rows="3" placeholder="Mandatory decision reasons" class="w-full rounded-xl border-slate-200"></textarea><div class="flex gap-3"><button name="decision" value="{{ $approve }}" class="rounded-xl bg-emerald-600 px-5 py-3 text-sm font-bold text-white">{{ $approveLabel }}</button><button name="decision" value="rejected" class="rounded-xl bg-red-50 px-5 py-3 text-sm font-bold text-red-700">Reject</button></div></form>
